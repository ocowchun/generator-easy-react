language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-easy-react' ]; then cd .. && eval "mv $currentfolder generator-easy-react" && cd generator-easy-react; fi

